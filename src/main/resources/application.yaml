server:
  port: 6868

management: # 开启监控
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    cn.yifan.drawsee.mapper : debug
    cn.yifan.drawsee.worker : info

sa-token:
  # token 名称（同时也是 cookie 名称）
  token-name: Authorization
  # token 有效期（单位：秒） 默认30天，-1 代表永久有效
  timeout: 2592000
  # token 最低活跃频率（单位：秒），如果 token 超过此时间没有访问系统就会被冻结，默认-1 代表不限制，永不冻结
  active-timeout: -1
  # 是否允许同一账号多地同时登录 （为 true 时允许一起登录, 为 false 时新登录挤掉旧登录）
  is-concurrent: true
  # 在多人登录同一账号时，是否共用一个 token （为 true 时所有登录共用一个 token, 为 false 时每次登录新建一个 token）
  is-share: true
  # token 风格（默认可取值：uuid、simple-uuid、random-32、random-64、random-128、tik）
  token-style: uuid
  # 是否输出操作日志
  is-log: true
  # token前缀
  token-prefix: Bearer

drawsee:
  mysql:
    driver-class-name: com.mysql.cj.jdbc.Driver
    host: rm-2ze68xa54uq7agma27o.mysql.rds.aliyuncs.com
    port: 3306
    database: drawsee
    username: root
    password: Funstack@3205
  minio:
    endpoint: http://115.191.37.253:9046
    accessKey: s0j3IBszn6Xg0eNkKbj7
    secretKey: v76MDrkFpSgfon7RhAfwqeUxarpsB2aF7QbyLuUa
    bucketName: drawsee
  models:
    qwen:
      baseUrl: https://dashscope.aliyuncs.com/compatible-mode/v1
      apiKey: sk-1f046e48086e4f1d88f9dc031b4cbe0a
      modelName: qwen3.5-plus
      temperature: 0.7
      customParameters:
        enable_thinking: false
    deepseekV3:
      baseUrl: https://api.siliconflow.cn/v1
      apiKey: sk-eeixoawctuiqwnjvyomhnclwsxylwpgisdyuvbgojgvvjwqm
      modelName: deepseek-ai/DeepSeek-V3.2
      customParameters:
        enable_thinking: false
    qwenVision:
      baseUrl: https://api.siliconflow.cn/v1
      apiKey: sk-eeixoawctuiqwnjvyomhnclwsxylwpgisdyuvbgojgvvjwqm
      modelName: Pro/moonshotai/Kimi-K2.5
      temperature: 0.1
      customParameters:
        enable_thinking: false
    compress:
      baseUrl: https://api.siliconflow.cn/v1
      apiKey: sk-eeixoawctuiqwnjvyomhnclwsxylwpgisdyuvbgojgvvjwqm
      modelName: deepseek-ai/DeepSeek-R1-Distill-Qwen-14B
      temperature: 0.2
    embedding:
      baseUrl: ${EMBEDDING_BASE_URL:https://api.siliconflow.cn/v1/embeddings}
      apiKey: ${EMBEDDING_API_KEY:sk-ljtiejfrkybzmtyauvvjaezbawsazvckhnsfxyejnqldlspv}
      modelName: ${EMBEDDING_MODEL:Qwen/Qwen3-Embedding-8B}
  # Web搜索配置（用于查询电子元器件资料）
  search:
    enabled: ${SEARCH_ENABLED:true}  # 是否启用搜索功能
    api-key: ${SEARCH_API_KEY:bce-v3/ALTAK-9uOVZ9zTecaAKTY3taFhJ/71ee329eea9a9df805e15aa310110dc13ddf8e71}  # 百度AppBuilder API Key，获取方式见文档
  # Python微服务配置
  python-service:
    base-url: http://localhost:8000  # Python服务地址（FastAPI默认端口8000）
    timeout: 60000  # 超时时间（毫秒）
    enabled: false  # 是否启用Python服务（v2.0生产就绪）
  # RAG入库流程配置
  rag-ingestion:
    mode: JAVA  # JAVA / PYTHON
    batch-size: 64
  # RAG检索流程配置
  rag-query:
    mode: JAVA  # JAVA / PYTHON
    top-k: 5
    max-context-tokens: 160000
    safe-input-ratio: 0.65
    reserved-output-tokens: 8000
    history-ratio: 0.30
    retrieval-ratio: 0.45
    max-chunks-in-context: 4
    rag-timeout-ms: 1200
  # Qdrant向量库配置（由Python RAG侧使用，Java侧预留）
  qdrant:
    host: ${QDRANT_HOST:115.191.37.253}
    port: ${QDRANT_PORT:6333}
    api-key: ${QDRANT_API_KEY:}
    https: ${QDRANT_HTTPS:false}
    collection: ${QDRANT_COLLECTION:circuit_embeddings}
  # 内部服务JWT配置
  internal-jwt:
    secret: ${INTERNAL_JWT_SECRET:your-super-secret-key-min-32-chars-change-in-production}  # 与Python共享的密钥（生产环境必须修改）
    expiration: 3600  # Token过期时间（秒）
  rabbitmq:
    aiConfig:
      exchangeName: direct_ai_task_exchange
      queueName: direct_ai_task_queue
      routingKey: ai_task
      queueCount: 5
    animationConfig:
      exchangeName: direct_animation_task_exchange
      queueName: direct_animation_task_queue
      routingKey: animation_task
      queueCount: 3

spring:
  application:
    name: drawsee-java
  profiles:
    active: prod
  mail:
    host: smtp.qq.com
    protocol: smtp
    default-encoding: UTF-8
    username: 3149107774@qq.com
    password: ofnkhtpclryjdgfe
    test-connection: true
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    encoding: UTF-8 #编码
    suffix: .html #模板后缀
    mode: HTML #模板
  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 100MB
      file-size-threshold: 2KB
      location: ${java.io.tmpdir}
      resolve-lazily: false
  rabbitmq:
    host: 115.191.37.253
    port: 5678
  datasource:
    druid:
      driver-class-name: ${drawsee.mysql.driver-class-name}
      url: jdbc:mysql://${drawsee.mysql.host}:${drawsee.mysql.port}/${drawsee.mysql.database}?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
      username: ${drawsee.mysql.username}
      password: ${drawsee.mysql.password}
  data:
    redis:
      host: 115.191.37.253
      port: 6278
      # 连接超时时间
      timeout: 10s
      lettuce:
        pool:
          # 连接池最大连接数
          max-active: 200
          # 连接池最大阻塞等待时间（使用负值表示没有限制）
          max-wait: -1ms
          # 连接池中的最大空闲连接
          max-idle: 10
          # 连接池中的最小空闲连接
          min-idle: 0

# RAG配置（已迁移到Python RAG微服务）
rag:
  chunk-size: ${RAG_CHUNK_SIZE:800}
  chunk-overlap: ${RAG_CHUNK_OVERLAP:200}

mybatis:
  #mapper配置文件
  mapper-locations: classpath*:mapper/*.xml # classpath*: 会搜索所有类路径（包括依赖 jar 包中的资源）
  type-aliases-package: cn.yifan.drawsee.pojo.entity
  configuration:
    #开启驼峰命名
    map-underscore-to-camel-case: true
