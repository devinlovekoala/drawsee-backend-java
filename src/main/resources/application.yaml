server:
  port: 6868

management: # 开启监控
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    cn.yifan.drawsee.mapper : debug
    cn.yifan.drawsee.worker : info

sa-token:
  # token 名称（同时也是 cookie 名称）
  token-name: Authorization
  # token 有效期（单位：秒） 默认30天，-1 代表永久有效
  timeout: 2592000
  # token 最低活跃频率（单位：秒），如果 token 超过此时间没有访问系统就会被冻结，默认-1 代表不限制，永不冻结
  active-timeout: -1
  # 是否允许同一账号多地同时登录 （为 true 时允许一起登录, 为 false 时新登录挤掉旧登录）
  is-concurrent: true
  # 在多人登录同一账号时，是否共用一个 token （为 true 时所有登录共用一个 token, 为 false 时每次登录新建一个 token）
  is-share: true
  # token 风格（默认可取值：uuid、simple-uuid、random-32、random-64、random-128、tik）
  token-style: uuid
  # 是否输出操作日志
  is-log: true
  # token前缀
  token-prefix: Bearer

drawsee:
  mysql:
    driver-class-name: com.mysql.cj.jdbc.Driver
    host: rm-2ze68xa54uq7agma27o.mysql.rds.aliyuncs.com
    port: 3306
    database: drawsee
    username: root
    password: Funstack@3205
  minio:
    endpoint: http://117.72.9.87:9046
    accessKey: 1r6Dfs4muAO6JfKFnkqH
    secretKey: ax1EHy6YBLFTDCTsRVcyxsz8JZoDaYZW29OozYTI
    bucketName: drawsee
  models:
    doubao:
      baseUrl: https://api.siliconflow.cn/v1
      apiKey: sk-eeixoawctuiqwnjvyomhnclwsxylwpgisdyuvbgojgvvjwqm
      modelName: moonshotai/Kimi-K2-Instruct
    deepseekV3:
      baseUrl: https://api.siliconflow.cn/v1
      apiKey: sk-eeixoawctuiqwnjvyomhnclwsxylwpgisdyuvbgojgvvjwqm
      modelName: deepseek-ai/DeepSeek-V3
    doubaoVision:
      baseUrl: https://ark.cn-beijing.volces.com/api/v3
      apiKey: a923c210-64ac-43db-b0b9-5122b11a6aa7
      modelName: doubao-seed-1-6-vision-250815
    embedding:
      baseUrl: ${EMBEDDING_BASE_URL:https://api.siliconflow.cn/v1}
      apiKey: ${EMBEDDING_API_KEY:sk-eeixoawctuiqwnjvyomhnclwsxylwpgisdyuvbgojgvvjwqm}
      modelName: ${EMBEDDING_MODEL:BAAI/bge-m3}
  rabbitmq:
    aiConfig:
      exchangeName: direct_ai_task_exchange
      queueName: direct_ai_task_queue
      routingKey: ai_task
      queueCount: 3
    animationConfig:
      exchangeName: direct_animation_task_exchange
      queueName: direct_animation_task_queue
      routingKey: animation_task
      queueCount: 3

spring:
  application:
    name: drawsee-java
  profiles:
    active: prod
  mail:
    host: smtp.qq.com
    protocol: smtp
    default-encoding: UTF-8
    username: 3149107774@qq.com
    password: ofnkhtpclryjdgfe
    test-connection: true
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    encoding: UTF-8 #编码
    suffix: .html #模板后缀
    mode: HTML #模板
  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 100MB
      file-size-threshold: 2KB
      location: ${java.io.tmpdir}
      resolve-lazily: false
  rabbitmq:
    host: 117.72.9.87
    port: 5678
  datasource:
    druid:
      driver-class-name: ${drawsee.mysql.driver-class-name}
      url: jdbc:mysql://${drawsee.mysql.host}:${drawsee.mysql.port}/${drawsee.mysql.database}?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
      username: ${drawsee.mysql.username}
      password: ${drawsee.mysql.password}
  data:
    redis:
      host: 117.72.9.87
      port: 6278
      # 连接超时时间
      timeout: 10s
      lettuce:
        pool:
          # 连接池最大连接数
          max-active: 200
          # 连接池最大阻塞等待时间（使用负值表示没有限制）
          max-wait: -1ms
          # 连接池中的最大空闲连接
          max-idle: 10
          # 连接池中的最小空闲连接
          min-idle: 0

rag:
  weaviate:
    endpoint: ${WEAVIATE_ENDPOINT:http://117.72.9.87:8080}
    api-key: ${WEAVIATE_API_KEY:}
    class-prefix: ${WEAVIATE_CLASS_PREFIX:KB}
    chunk-size: ${WEAVIATE_CHUNK_SIZE:800}
    chunk-overlap: ${WEAVIATE_CHUNK_OVERLAP:200}

mybatis:
  #mapper配置文件
  mapper-locations: classpath*:mapper/*.xml # classpath*: 会搜索所有类路径（包括依赖 jar 包中的资源）
  type-aliases-package: cn.yifan.drawsee.pojo.entity
  configuration:
    #开启驼峰命名
    map-underscore-to-camel-case: true
